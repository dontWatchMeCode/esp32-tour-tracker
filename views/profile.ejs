<%- include('partials/header') -%>

    <h1>Profile</h1>

    <% if (user.picture) { %>
        <img src="<%= user.picture %>" width="300">
        <% } %>

            <p>
                <code>req.user</code>
            </p>

            <pre><%= userProfile %></pre>

            <hr>

            <%- include('partials/footer') -%>